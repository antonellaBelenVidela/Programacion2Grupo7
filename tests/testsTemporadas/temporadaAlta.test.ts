import TemporadaAlta from "../../src/temporadas/temporadaAlta";

describe("Test Unitario de la clase TemporadaAlta", () => {
    let estado: TemporadaAlta;

    beforeEach(() => {
        estado = new TemporadaAlta();
    });

    test(" ", () => {
 
    });

});